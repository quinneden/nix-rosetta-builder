images:
- location: "result/nixos.qcow2"

cpus: 8
memory: "6GiB"

mounts:
  - location: "~/rosetta-builder/sshdkeys" # must be first so it matches `mount0` tag in flake.nix

ssh:
  localPort: 2226

rosetta:
  enabled: true
