images:
- location: "result/nixos.qcow2"

cpus: 8
memory: "6GiB"

mounts:
  - location: "~/lima-builder-keys" # must be first to match `mount0` tag in flake.nix

ssh:
  localPort: 2226

rosetta:
  enabled: true
